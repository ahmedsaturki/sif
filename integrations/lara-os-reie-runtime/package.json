{
  "name": "lara-os-reie-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free local-first REIE runtime with an explicit SIF governance boundary.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
