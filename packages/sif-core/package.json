{
  "name": "sif-core",
  "version": "0.5.0",
  "private": false,
  "description": "Dependency-free TypeScript core for a Sovereign Intelligence Fabric with transactional PostgreSQL integration, durable outbox, cryptographic integrity, CAS, evidence/provenance, scoped authority, policy admission, semantic knowledge, lineage and reconstruction verification.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "packageManager": "npm@10.9.8",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "sql",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "npm test",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}